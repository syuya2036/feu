[package]
name = "feu-core"
version = "0.1.0"
edition = "2021"

[dependencies]
http = "1.0"
bytes = "1.0"
thiserror = "1.0"
feu-router = { path = "../feu-router" }

[features]
default = []
openapi = []
rpc = []

[dev-dependencies]
tokio = { version = "1.0", features = ["macros", "rt"] }
